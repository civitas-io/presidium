package bench

import rego.v1

default allow := false

# distractor 0 -- never matches the benchmark request
deny_reason_0 if input.resource == "tool:never-matches-0"

# distractor 1 -- never matches the benchmark request
deny_reason_1 if input.resource == "tool:never-matches-1"

# distractor 2 -- never matches the benchmark request
deny_reason_2 if input.resource == "tool:never-matches-2"

# distractor 3 -- never matches the benchmark request
deny_reason_3 if input.resource == "tool:never-matches-3"

# distractor 4 -- never matches the benchmark request
deny_reason_4 if input.resource == "tool:never-matches-4"

# distractor 5 -- never matches the benchmark request
deny_reason_5 if input.resource == "tool:never-matches-5"

# distractor 6 -- never matches the benchmark request
deny_reason_6 if input.resource == "tool:never-matches-6"

# distractor 7 -- never matches the benchmark request
deny_reason_7 if input.resource == "tool:never-matches-7"

# distractor 8 -- never matches the benchmark request
deny_reason_8 if input.resource == "tool:never-matches-8"

# distractor 9 -- never matches the benchmark request
deny_reason_9 if input.resource == "tool:never-matches-9"

# distractor 10 -- never matches the benchmark request
deny_reason_10 if input.resource == "tool:never-matches-10"

# distractor 11 -- never matches the benchmark request
deny_reason_11 if input.resource == "tool:never-matches-11"

# distractor 12 -- never matches the benchmark request
deny_reason_12 if input.resource == "tool:never-matches-12"

# distractor 13 -- never matches the benchmark request
deny_reason_13 if input.resource == "tool:never-matches-13"

# distractor 14 -- never matches the benchmark request
deny_reason_14 if input.resource == "tool:never-matches-14"

# distractor 15 -- never matches the benchmark request
deny_reason_15 if input.resource == "tool:never-matches-15"

# distractor 16 -- never matches the benchmark request
deny_reason_16 if input.resource == "tool:never-matches-16"

# distractor 17 -- never matches the benchmark request
deny_reason_17 if input.resource == "tool:never-matches-17"

# distractor 18 -- never matches the benchmark request
deny_reason_18 if input.resource == "tool:never-matches-18"

# distractor 19 -- never matches the benchmark request
deny_reason_19 if input.resource == "tool:never-matches-19"

# distractor 20 -- never matches the benchmark request
deny_reason_20 if input.resource == "tool:never-matches-20"

# distractor 21 -- never matches the benchmark request
deny_reason_21 if input.resource == "tool:never-matches-21"

# distractor 22 -- never matches the benchmark request
deny_reason_22 if input.resource == "tool:never-matches-22"

# distractor 23 -- never matches the benchmark request
deny_reason_23 if input.resource == "tool:never-matches-23"

# distractor 24 -- never matches the benchmark request
deny_reason_24 if input.resource == "tool:never-matches-24"

# distractor 25 -- never matches the benchmark request
deny_reason_25 if input.resource == "tool:never-matches-25"

# distractor 26 -- never matches the benchmark request
deny_reason_26 if input.resource == "tool:never-matches-26"

# distractor 27 -- never matches the benchmark request
deny_reason_27 if input.resource == "tool:never-matches-27"

# distractor 28 -- never matches the benchmark request
deny_reason_28 if input.resource == "tool:never-matches-28"

# distractor 29 -- never matches the benchmark request
deny_reason_29 if input.resource == "tool:never-matches-29"

# distractor 30 -- never matches the benchmark request
deny_reason_30 if input.resource == "tool:never-matches-30"

# distractor 31 -- never matches the benchmark request
deny_reason_31 if input.resource == "tool:never-matches-31"

# distractor 32 -- never matches the benchmark request
deny_reason_32 if input.resource == "tool:never-matches-32"

# distractor 33 -- never matches the benchmark request
deny_reason_33 if input.resource == "tool:never-matches-33"

# distractor 34 -- never matches the benchmark request
deny_reason_34 if input.resource == "tool:never-matches-34"

# distractor 35 -- never matches the benchmark request
deny_reason_35 if input.resource == "tool:never-matches-35"

# distractor 36 -- never matches the benchmark request
deny_reason_36 if input.resource == "tool:never-matches-36"

# distractor 37 -- never matches the benchmark request
deny_reason_37 if input.resource == "tool:never-matches-37"

# distractor 38 -- never matches the benchmark request
deny_reason_38 if input.resource == "tool:never-matches-38"

# distractor 39 -- never matches the benchmark request
deny_reason_39 if input.resource == "tool:never-matches-39"

# distractor 40 -- never matches the benchmark request
deny_reason_40 if input.resource == "tool:never-matches-40"

# distractor 41 -- never matches the benchmark request
deny_reason_41 if input.resource == "tool:never-matches-41"

# distractor 42 -- never matches the benchmark request
deny_reason_42 if input.resource == "tool:never-matches-42"

# distractor 43 -- never matches the benchmark request
deny_reason_43 if input.resource == "tool:never-matches-43"

# distractor 44 -- never matches the benchmark request
deny_reason_44 if input.resource == "tool:never-matches-44"

# distractor 45 -- never matches the benchmark request
deny_reason_45 if input.resource == "tool:never-matches-45"

# distractor 46 -- never matches the benchmark request
deny_reason_46 if input.resource == "tool:never-matches-46"

# distractor 47 -- never matches the benchmark request
deny_reason_47 if input.resource == "tool:never-matches-47"

# distractor 48 -- never matches the benchmark request
deny_reason_48 if input.resource == "tool:never-matches-48"

# distractor 49 -- never matches the benchmark request
deny_reason_49 if input.resource == "tool:never-matches-49"

# distractor 50 -- never matches the benchmark request
deny_reason_50 if input.resource == "tool:never-matches-50"

# distractor 51 -- never matches the benchmark request
deny_reason_51 if input.resource == "tool:never-matches-51"

# distractor 52 -- never matches the benchmark request
deny_reason_52 if input.resource == "tool:never-matches-52"

# distractor 53 -- never matches the benchmark request
deny_reason_53 if input.resource == "tool:never-matches-53"

# distractor 54 -- never matches the benchmark request
deny_reason_54 if input.resource == "tool:never-matches-54"

# distractor 55 -- never matches the benchmark request
deny_reason_55 if input.resource == "tool:never-matches-55"

# distractor 56 -- never matches the benchmark request
deny_reason_56 if input.resource == "tool:never-matches-56"

# distractor 57 -- never matches the benchmark request
deny_reason_57 if input.resource == "tool:never-matches-57"

# distractor 58 -- never matches the benchmark request
deny_reason_58 if input.resource == "tool:never-matches-58"

# distractor 59 -- never matches the benchmark request
deny_reason_59 if input.resource == "tool:never-matches-59"

# distractor 60 -- never matches the benchmark request
deny_reason_60 if input.resource == "tool:never-matches-60"

# distractor 61 -- never matches the benchmark request
deny_reason_61 if input.resource == "tool:never-matches-61"

# distractor 62 -- never matches the benchmark request
deny_reason_62 if input.resource == "tool:never-matches-62"

# distractor 63 -- never matches the benchmark request
deny_reason_63 if input.resource == "tool:never-matches-63"

# distractor 64 -- never matches the benchmark request
deny_reason_64 if input.resource == "tool:never-matches-64"

# distractor 65 -- never matches the benchmark request
deny_reason_65 if input.resource == "tool:never-matches-65"

# distractor 66 -- never matches the benchmark request
deny_reason_66 if input.resource == "tool:never-matches-66"

# distractor 67 -- never matches the benchmark request
deny_reason_67 if input.resource == "tool:never-matches-67"

# distractor 68 -- never matches the benchmark request
deny_reason_68 if input.resource == "tool:never-matches-68"

# distractor 69 -- never matches the benchmark request
deny_reason_69 if input.resource == "tool:never-matches-69"

# distractor 70 -- never matches the benchmark request
deny_reason_70 if input.resource == "tool:never-matches-70"

# distractor 71 -- never matches the benchmark request
deny_reason_71 if input.resource == "tool:never-matches-71"

# distractor 72 -- never matches the benchmark request
deny_reason_72 if input.resource == "tool:never-matches-72"

# distractor 73 -- never matches the benchmark request
deny_reason_73 if input.resource == "tool:never-matches-73"

# distractor 74 -- never matches the benchmark request
deny_reason_74 if input.resource == "tool:never-matches-74"

# distractor 75 -- never matches the benchmark request
deny_reason_75 if input.resource == "tool:never-matches-75"

# distractor 76 -- never matches the benchmark request
deny_reason_76 if input.resource == "tool:never-matches-76"

# distractor 77 -- never matches the benchmark request
deny_reason_77 if input.resource == "tool:never-matches-77"

# distractor 78 -- never matches the benchmark request
deny_reason_78 if input.resource == "tool:never-matches-78"

# distractor 79 -- never matches the benchmark request
deny_reason_79 if input.resource == "tool:never-matches-79"

# distractor 80 -- never matches the benchmark request
deny_reason_80 if input.resource == "tool:never-matches-80"

# distractor 81 -- never matches the benchmark request
deny_reason_81 if input.resource == "tool:never-matches-81"

# distractor 82 -- never matches the benchmark request
deny_reason_82 if input.resource == "tool:never-matches-82"

# distractor 83 -- never matches the benchmark request
deny_reason_83 if input.resource == "tool:never-matches-83"

# distractor 84 -- never matches the benchmark request
deny_reason_84 if input.resource == "tool:never-matches-84"

# distractor 85 -- never matches the benchmark request
deny_reason_85 if input.resource == "tool:never-matches-85"

# distractor 86 -- never matches the benchmark request
deny_reason_86 if input.resource == "tool:never-matches-86"

# distractor 87 -- never matches the benchmark request
deny_reason_87 if input.resource == "tool:never-matches-87"

# distractor 88 -- never matches the benchmark request
deny_reason_88 if input.resource == "tool:never-matches-88"

# distractor 89 -- never matches the benchmark request
deny_reason_89 if input.resource == "tool:never-matches-89"

# distractor 90 -- never matches the benchmark request
deny_reason_90 if input.resource == "tool:never-matches-90"

# distractor 91 -- never matches the benchmark request
deny_reason_91 if input.resource == "tool:never-matches-91"

# distractor 92 -- never matches the benchmark request
deny_reason_92 if input.resource == "tool:never-matches-92"

# distractor 93 -- never matches the benchmark request
deny_reason_93 if input.resource == "tool:never-matches-93"

# distractor 94 -- never matches the benchmark request
deny_reason_94 if input.resource == "tool:never-matches-94"

# distractor 95 -- never matches the benchmark request
deny_reason_95 if input.resource == "tool:never-matches-95"

# distractor 96 -- never matches the benchmark request
deny_reason_96 if input.resource == "tool:never-matches-96"

# distractor 97 -- never matches the benchmark request
deny_reason_97 if input.resource == "tool:never-matches-97"

# distractor 98 -- never matches the benchmark request
deny_reason_98 if input.resource == "tool:never-matches-98"

allow if {
    input.resource == "tool:benchmark-target"
    not deny_reason_0
    not deny_reason_1
    not deny_reason_2
    not deny_reason_3
    not deny_reason_4
    not deny_reason_5
    not deny_reason_6
    not deny_reason_7
    not deny_reason_8
    not deny_reason_9
    not deny_reason_10
    not deny_reason_11
    not deny_reason_12
    not deny_reason_13
    not deny_reason_14
    not deny_reason_15
    not deny_reason_16
    not deny_reason_17
    not deny_reason_18
    not deny_reason_19
    not deny_reason_20
    not deny_reason_21
    not deny_reason_22
    not deny_reason_23
    not deny_reason_24
    not deny_reason_25
    not deny_reason_26
    not deny_reason_27
    not deny_reason_28
    not deny_reason_29
    not deny_reason_30
    not deny_reason_31
    not deny_reason_32
    not deny_reason_33
    not deny_reason_34
    not deny_reason_35
    not deny_reason_36
    not deny_reason_37
    not deny_reason_38
    not deny_reason_39
    not deny_reason_40
    not deny_reason_41
    not deny_reason_42
    not deny_reason_43
    not deny_reason_44
    not deny_reason_45
    not deny_reason_46
    not deny_reason_47
    not deny_reason_48
    not deny_reason_49
    not deny_reason_50
    not deny_reason_51
    not deny_reason_52
    not deny_reason_53
    not deny_reason_54
    not deny_reason_55
    not deny_reason_56
    not deny_reason_57
    not deny_reason_58
    not deny_reason_59
    not deny_reason_60
    not deny_reason_61
    not deny_reason_62
    not deny_reason_63
    not deny_reason_64
    not deny_reason_65
    not deny_reason_66
    not deny_reason_67
    not deny_reason_68
    not deny_reason_69
    not deny_reason_70
    not deny_reason_71
    not deny_reason_72
    not deny_reason_73
    not deny_reason_74
    not deny_reason_75
    not deny_reason_76
    not deny_reason_77
    not deny_reason_78
    not deny_reason_79
    not deny_reason_80
    not deny_reason_81
    not deny_reason_82
    not deny_reason_83
    not deny_reason_84
    not deny_reason_85
    not deny_reason_86
    not deny_reason_87
    not deny_reason_88
    not deny_reason_89
    not deny_reason_90
    not deny_reason_91
    not deny_reason_92
    not deny_reason_93
    not deny_reason_94
    not deny_reason_95
    not deny_reason_96
    not deny_reason_97
    not deny_reason_98
}
